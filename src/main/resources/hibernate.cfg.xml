<?xml version="1.0" encoding="UTF-8"?>
<hibernate-configuration>
    <session-factory>
        <!-- Database connection settings -->
        <property name = "connection.driver_class">com.microsoft.sqlserver.jdbc.SQLServerDriver</property>
        <property name = "connection.url">jdbc:sqlserver://localhost:6725;database=myschool</property>
        <!-- JDBC connection pool (use the built-in) -->
        <!--
        <property name="connection.pool_size">1</property>
         -->
        <!-- SQL dialect -->
        <property name = "dialect">org.hibernate.dialect.MySQLDialect</property>
        <!-- Enable Hibernate's automatic session context management -->
        <property name = "current_session_context_class">thread</property>

        <!-- Echo all executed SQL to stdout -->
        <property name = "show_sql">true</property>
        <!-- Drop and re-create the database schema on startup -->
        <!--
        <property name="hbm2ddl.auto">update</property>
        -->

        <mapping class="org.school.model.CollegeModel"/>
        <mapping class="org.school.model.DepartmentModel"/>
        <mapping class="org.school.model.EventModel"/>
        <mapping class="org.school.model.EventTypeModel"/>
        <mapping class="org.school.model.UserModel"/>
        <mapping class="org.school.model.UserTypeModel"/>
    </session-factory>


</hibernate-configuration>